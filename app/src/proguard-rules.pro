# Add project specific ProGuard rules here.
#
# Minification is currently disabled for the release build type (see
# app/build.gradle.kts) since this is a small, sideloaded personal-use
# app where APK size and obfuscation aren't priorities. This file is
# kept so the build.gradle.kts proguardFiles() reference resolves, and
# so rules are easy to add later if you turn minification on.
